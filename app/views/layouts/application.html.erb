<!DOCTYPE html>
<html>
<head>
  <title>Wandermust</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

  <div class="navbar navbar-inverse navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">

        <!-- .btn-navbar is used as the toggle for collapsed navbar content -->
        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">

          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>

        <!-- Be sure to leave the brand out there if you want it shown -->
        <a class="brand" href="/">Wandermust</a>

        <!-- Everything you want hidden at 940px or less, place within here -->
        <div class="nav-collapse collapse">
          <ul class="nav pull-right">
            <% if current_user %>
              <li class=""><%= link_to "Howdy, #{current_user.first_name}", user_path(current_user), :class => "nav" %></li>
              <li class=""><%= link_to "Log Out", logout_path, :class => "nav" %></li>
            <% else %>
              <li class=""><%= link_to "Signup", login_path, :class => "nav" %></li>
              <li class=""><%= link_to "Log In", login_path, :class => "nav" %></li>
            <% end %>
          </ul>
          <!-- .nav, .navbar-search, .navbar-form, etc -->
        </div>

      </div>
    </div>
  </div>
  <div class="container-fluid">

    <% unless flash[:notice].blank? %>
      <p id="flash_notice"><%= flash[:notice] %></p>
    <% end %>

    <% unless flash[:alert].blank? %>
      <p id="flash_alert"><%= flash[:alert] %></p>
    <% end %>

    <div class="row-fluid">
      <div class="span9 well">

        <%= yield %>

      </div>
      <div class="span3 well">
        <ul class="nav nav-list">
          <li class="nav-header">Top Saved Destinations</li>
          <li class="divider"></li>
          <!-- This will be where I list the top destinations by their instances on the bridge table -->
          <li><a href="#">Paris</a></li>
          <li><a href="#">Hong Kong</a></li>
          <li class="nav-header">Trending Destinations</li>
          <!-- This will be saved by timestamp over the last 30 days -->
          <li class="divider"></li>
          <li class="active"><a href="#">Istanbul</a></li> <!-- if you are on that page, it's active -->
          <li><a href="#">Absecon</a></li>
        </ul>
      </div>
    </div>
  </div>





</body>
</html>
