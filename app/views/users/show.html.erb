<h1>Your Saved Destinations</h1>

<table>
  <tr>
    <th>Destination</th>
    <th>Coords</th>
    <th></th>
    <th></th>
   <!--  <th></th> -->
  </tr>

<% @user.destinations.each do |destination| %>
  <tr>
    <td><%= destination.address %></td>
    <td><%= destination.latitude %> and <%= destination.longitude %></td>
    <td><%= link_to 'Show', destination %></td>
<!--     <td><%#= link_to 'Destroy', user.destination.pop?, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
  </tr>
<% end %>
</table>

<br />


<%= link_to 'Edit your Profile', edit_user_path(@user) %> |
<%= link_to 'Back to Home', destinations_path %>
